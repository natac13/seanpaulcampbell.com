---
import { cn } from '../lib/utils'

const { class: className } = Astro.props
---

<span
  class={cn(
    "text-xs font-sans font-medium bg-muted text-muted-foreground px-2.5 py-1 rounded-full transition-colors",
    className
  )}
>
  <slot />
</span>
