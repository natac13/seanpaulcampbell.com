---
import { cn } from '../lib/utils'

const { class: className } = Astro.props
---

<span
  class={cn(
    "text-xs font-medium bg-muted text-muted-foreground px-2 py-1 rounded-full hover:bg-primary/80 hover:text-primary-foreground transition-colors",
    className
  )}
>
  <slot />
</span>
